#include <bits/stdc++.h>
#include <iostream>

using namespace std;

// disjoint set union (union find) - o(n)
// path compression is a way to decrese time complexity - o(logn)
// union by size/rank - o(alpha(n))
/*
application:
1. MST
2.connected component in a graph
3. cycle detection and many more
*/


int main() {

  

  return 0;
}
