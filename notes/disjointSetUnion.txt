1. disjoint set union (union find) - o(n)
2. path compression is a way to decrese time complexity - o(logn)
3. union by size/rank - o(alpha(n))

application:
1. MST
2. connected component in a graph
3. cycle detection and many more
